# 上线操作清单（中文版）

> 目标路径：**Cloudflare 买域名 → GitHub 推代码 → Cloudflare Pages 部署 → 绑定域名 → 替换占位符 → 验证**
> 预计总耗时：**60–90 分钟**（大部分是你账号注册的时间）
> 我这边已完成：Git 仓库初始化 + 首次提交（25 个文件），推送到 GitHub 只差 3 条命令。

---

## 你需要准备的账号（3 个）

| 账号 | 地址 | 用途 | 备注 |
|------|------|------|------|
| Cloudflare | dash.cloudflare.com/sign-up | 买域名 + Pages 托管 | 免费 |
| GitHub | github.com/signup | 存放代码 | 免费 |
| （可选）Amazon Associates | affiliate-program.amazon.com | 联盟佣金 | 网站上线后再注册 |

> 💡 国内网络访问 GitHub 和 Cloudflare 可能较慢，建议全程开着代理。

---

## 第 1 步：在 Cloudflare 买域名（约 15 分钟）

1. 打开 https://dash.cloudflare.com ，注册账号（邮箱验证）
2. 左侧菜单 → **Domain Registration（域名注册）→ Register Domains**
3. 搜索你想要的域名，比如 `hearthandhome`，看 `.com` 是否可买
   - 建议优先 `.com`（Amazon 审核和欧美用户都最认 .com）
   - `.com` 约 $10–11/年，Cloudflare 按成本价卖，无溢价
   - 如果 `.com` 被占了，备选：`hearthandhomehq.com`、`hearthhome.com`、`hearthandhomereviews.com`
4. 付款：Cloudflare 只支持**信用卡或 PayPal**（不支持支付宝/微信）
   - 没有国际卡的话 → 换 Namecheap（支持部分国内支付方式）或用万网（阿里云，支持支付宝），买完后 DNS 托管到 Cloudflare 也可以，我可以给你迁移步骤
5. 域名买好后，Cloudflare 会自动把它加入你的账户（DNS 也自动在 Cloudflare 管理）

✅ **验证方式**：在浏览器打开 `https://你的域名.com`，应该显示 Cloudflare 的"站点未配置"页面（这是正常的，部署完成后就有内容了）

---

## 第 2 步：注册 GitHub 并建仓库（约 10 分钟）

1. 打开 https://github.com/signup ，注册（用户名建议取和域名相关的，比如 `hearth-home`）
2. 验证邮箱
3. 点右上角 **`+` → New repository**
4. 仓库名填：`hearth-home-reviews`（名字随意，记住就行）
5. 选 **Private**（私有）或 **Public** 都可以 —— Pages 两种都支持，**推荐 Public**，省事
6. **不要**勾选 "Add a README"、".gitignore"、"license"（我们本地已有）
7. 点 **Create repository**

✅ **验证方式**：浏览器出现一个空仓库页面，地址类似 `https://github.com/你的用户名/hearth-home-reviews`

---

## 第 3 步：把代码推上去（约 5 分钟）

GitHub 建好仓库后，页面会显示一串命令。在你 Mac 的终端里执行（把 `你的用户名` 换成实际值）：

```bash
cd /Users/fyc/WorkBuddy/2026-09-20-13-12-32/amazon-furniture-affiliate

# ① 先把 git 身份改成你的 GitHub 信息（提交记录会用它）
git config user.name "你的GitHub用户名"
git config user.email "你的GitHub邮箱@xxx.com"

# ② 连接远程仓库
git remote add origin https://github.com/你的用户名/hearth-home-reviews.git

# ③ 推送
git push -u origin main
```

> 第一次推送时会弹出 GitHub 登录窗口，按提示用浏览器授权即可（macOS 自带 git 支持）。
> 如果弹的是要求输入密码的窗口——**GitHub 已不支持密码**，需要用 Personal Access Token：
> GitHub → Settings → Developer settings → Personal access tokens → Generate new token，勾选 `repo` 权限，把生成的 token 当密码粘贴。

✅ **验证方式**：刷新 GitHub 仓库页面，能看到 25 个文件（index.html、reviews/、images/ 等）

---

## 第 4 步：Cloudflare Pages 部署（约 10 分钟）

1. 打开 https://dash.cloudflare.com ，进入左侧 **Workers & Pages**
2. 点 **Create → Pages → Connect to Git**
3. 授权 Cloudflare 访问你的 GitHub，选中 `hearth-home-reviews` 仓库，点 **Begin setup**
4. 构建设置（重要！）：
   - Project name：`hearth-home-reviews`（会成为临时域名）
   - Production branch：`main`
   - **Framework preset：None**
   - **Build command：留空**
   - **Build output directory：`/`**（如果填 `/` 报错，就改成 `.`）
5. 点 **Save and Deploy**，等 1–2 分钟

✅ **验证方式**：Cloudflare 会给你一个 `https://hearth-home-reviews.pages.dev` 的地址，打开能看到完整的网站首页

---

## 第 5 步：绑定你的域名（约 10 分钟）

1. 还是在 Pages 项目页 → **Custom domains** 标签
2. 点 **Set up a custom domain**，输入你第 1 步买的域名（如 `hearthandhome.com`）
3. 因为域名就在同一个 Cloudflare 账户里，DNS 记录**自动配置**，不用手动改任何东西
4. 等 5–10 分钟，SSL 证书自动签发

✅ **验证方式**：浏览器打开 `https://你的域名.com`，看到完整网站，地址栏有 🔒 锁标志

---

## 第 6 步：替换占位符（5 分钟，可以交给我）

现在网站代码里的链接还是 `https://example.com` 占位符。**告诉我你的最终域名**，我帮你执行全局替换并重新提交推送，例如：

```bash
# 域名确定后运行（把 YOUR-DOMAIN 换成实际域名，不带 https:// 前缀）
cd /Users/fyc/WorkBuddy/2026-09-20-13-12-32/amazon-furniture-affiliate
find . -type f \( -name "*.html" -o -name "*.xml" -o -name "*.txt" \) \
  -exec sed -i '' 's|https://example.com|https://YOUR-DOMAIN|g' {} +
git add -A && git commit -m "Replace placeholder domain with real domain" && git push
```

推完之后 Cloudflare Pages **自动重新部署**，1 分钟后生效。

---

## 第 7 步：上线后验证 + 收尾（约 15 分钟）

1. **全站检查**：打开首页 → 逐个点开 5 篇测评 → 点 About/Disclosure/Privacy/Terms/Contact，确认无死链、图片正常
2. **搜索引擎收录**：
   - Google Search Console：https://search.google.com/search-console → 添加资源 → 验证 → 提交 `sitemap.xml`
   - Bing Webmaster Tools：https://www.bing.com/webmasters
3. **测速**：https://pagespeed.web.dev/ 输入你的域名，Performance 和 SEO 都应该在 90+
4. **注册 Amazon Associates**（这时候填的网址就是合格的了）：
   - 网址填：`https://你的域名.com`（这就是之前报"格式不正确"的正解）
   - 通过后把 5 篇测评里 `href="#"` 的按钮换成带 Tracking ID 的真实链接

---

## 常见问题速查

| 现象 | 原因 / 解决 |
|------|------------|
| `git push` 报 403 | 用了密码而不是 token；或 token 没勾 `repo` 权限 |
| Pages 部署后 404 | Build output directory 填错了，改成 `/` 或 `.` 重试 |
| 绑定域名后打不开 | 等 10 分钟 DNS + SSL；确认域名和 Pages 在同一 Cloudflare 账户 |
| pages.dev 能开、自己的域名打不开 | 检查 Custom domains 里域名状态是否为 Active |
| 国内访问慢 | 正常（Cloudflare 国际节点），欧美用户访问速度不受影响 |

---

## 推荐域名备选清单

如果 `hearthandhome.com` 被注册，按顺序试：

1. `hearthandhomereviews.com`
2. `hearthhome.com`
3. `hearthandhomehq.com`
4. `thehearthandhome.com`
5. `hearth-home.com`（带连字符，次选）

---

*项目路径：`/Users/fyc/WorkBuddy/2026-09-20-13-12-32/amazon-furniture-affiliate/`*
*Git 状态：已初始化，主分支 `main`，首次提交 `0e9707b`（25 个文件）*
